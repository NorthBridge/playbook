from .. import export
import unittest

class ExportTest(unittest.TestCase):
    
    def test(self):
        #Set up the environment for the test
        #invoke the export script
        #export.main()
        #check if all stuff were done correctly
        #Clean up the environment after the test
        pass